name: CI

on:
  push:
    branches:
      - master
  workflow_dispatch:
          - plan

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello"
